# TermBridge 产品路线图

> 基线：v2.0.49 · 生成于 2026-08-23
> 规划方式：滚动路线图。时间窗从路线图启动之日计算，版本号表示目标主题，不等同于固定发布日期。

## 产品判断

TermBridge 当前已经具备四条清晰的产品主链路：

1. 连接与凭证：SSH / 本地终端、密码与私钥、系统钥匙串、跳板机、Known Hosts、自动重连。
2. 终端工作区：多标签、固定与排序、标签配色、分屏、快捷键、会话恢复。
3. 文件工作区：本地 / 远程双栏 SFTP、多标签、拖拽、预览、属性、书签、冲突处理、可取消传输和远程复制续传。
4. 运维辅助：应用与连接监控、日志、AI 对话、终端解释、命令生成和需逐步批准的只读诊断 Agent。

因此，下一阶段的重点不应是继续横向增加协议，而是把现有能力收束成一个可靠、连贯、可恢复、可审计的个人运维工作区。

## 北极星

让用户从“找到主机”到“定位并安全处理问题”的全过程，都能在一个可信的桌面工作区内完成。

核心承诺：

- 可靠：长时间会话、弱网重连、大文件传输和应用升级可预期。
- 高效：连接、终端、SFTP、转发、监控与 AI 共享同一主机上下文。
- 安全：秘密不离开安全存储；高风险动作始终可见、可控、可追溯。
- 轻量：保持独立桌面工具定位，不演变为重量级 IDE 或强依赖云端的管理平台。

## 优先级原则

按以下顺序处理需求：

1. 数据、凭证或远端环境安全。
2. 会话、传输、恢复和升级可靠性。
3. 高频运维任务的路径缩短。
4. AI 与自动化的可控性和可解释性。
5. 新平台、新协议和协作能力。

任何新能力都必须同时给出失败路径、取消或恢复方式、测试策略与安全边界。

---

## NOW — 可信基础（0–6 周，2.1 目标）

目标：让现有能力达到可以每天依赖的稳定程度，为后续工作区整合建立质量底座。

### 1. 质量门禁

- 为 pull request 与主干提交增加常规 CI：TypeScript 构建、前端测试、Rust 格式检查、Clippy 和 Rust 测试。
- 在 Windows 与 macOS 上运行关键平台测试；修复路径格式、钥匙串和本地文件行为的跨平台差异。
- 清理测试中的 React `act(...)`、Canvas 与 i18n 初始化噪声，使新增告警可被识别，而不是淹没在日志中。
- 建立最小端到端测试环境，使用本地隔离的 SSH / SFTP 服务覆盖连接、主机密钥、终端输入、上传下载、取消与重连。
- 为数据库迁移、自动更新清单和旧配置兼容建立版本化回归样例。

### 2. 工作区恢复

- 完成终端标签、分屏结构、固定状态与会话身份的崩溃安全持久化。
- 启动后以“已断开、可重连”方式恢复远程会话，不伪造连接仍然存活。
- 正确处理已删除连接、已丢失钥匙串条目、跳板机变化和版本升级后的陈旧状态。
- 保存并恢复 SFTP 标签、左右栏来源、路径、书签与分栏比例；不自动恢复可能产生副作用的传输。
- 提供“重新打开上次工作区”和“清除恢复状态”的明确入口。

### 3. 传输与文件安全

- 统一上传、下载、跨远端复制、批量删除的队列状态和取消语义。
- 对可续传任务保存必要元数据，并在重启后提供人工确认的恢复入口。
- 对覆盖操作采用临时文件、校验与原子替换；失败后保留原文件并可清理临时产物。
- 增加超大目录、深层目录、权限变化、磁盘不足、网络中断和同名冲突的端到端覆盖。
- 在退出、关闭标签和升级重启前集中展示仍在运行的传输与潜在影响。

### 4. AI 安全基线

- 将 AI API 密钥纳入系统钥匙串，配置数据库只保存非敏感元数据。
- 在发送上下文前提供可见范围与预览；默认遮蔽密码、令牌、私钥、环境变量秘密和疑似凭证。
- 保持诊断 Agent 只读、命令白名单、命令数量上限和逐步批准机制，并为绕过、拼接、重定向等场景补充安全测试。
- 明确区分“插入终端”和“执行命令”；未经用户动作不得发送回车执行。
- AI 会话归档、日志导出与错误信息遵守相同的脱敏规则。

### 5. 可诊断性

- 建立统一的操作 ID，串联连接、SFTP、端口转发、AI 请求与前后端日志。
- 提供一键生成脱敏诊断包，包含版本、平台、功能状态和用户主动选择的日志。
- 给连接、传输和更新失败提供稳定错误类别、可行动提示与重试入口。

### 退出条件

- Windows 与 macOS 的常规 CI 全绿，前后端测试无已知失败，测试告警有明确基线。
- 五条关键链路具备端到端测试：首次连接、会话重连、文件上传下载、工作区恢复、更新前退出保护。
- 崩溃或强制退出后不会造成配置损坏、秘密落盘或远端文件被静默截断。
- AI 上下文和诊断包通过秘密泄露回归测试。

独立审计状态（2026-08-24）：NOW 的五个已承诺工作流均有实现、失败/恢复语义和自动化证据，仓库内状态为 `verified`；本次同时关闭 Known Hosts 路径降级、AI API Key 反向迁移和终端恢复无版本/无上限三个安全缺口。第一条阶段退出条件仍为 `pending-external`：当前审计提交尚未进入托管平台 Windows/macOS 必需检查，因此不预先宣称 NOW 阶段整体通过。精确逐项映射见 `docs/roadmap-audit.json`。

---

## NEXT — 统一主机工作区（6–12 周，2.2 目标）

目标：从三个相邻功能区升级为围绕主机与任务组织的连续工作流。

### 1. 连接资产组织

- 为连接增加分组、标签、收藏、备注和最近活动筛选。
- 支持从 OpenSSH config 导入主机、别名、端口、用户、IdentityFile 与 ProxyJump；秘密仍单独进入钥匙串。
- 提供可预览、可选择、可回滚的连接配置导入 / 导出，默认不包含秘密。
- 在创建连接前提供可解释的连通性检查：DNS、TCP、主机密钥、认证和跳板链路分步呈现。

### 2. 主机上下文贯通

- 以连接配置作为稳定主机身份，使终端、SFTP、AI 会话和连接健康共享上下文。
- 从终端一键在同一主机打开 SFTP，从 SFTP 一键在当前目录打开终端。
- 在主机概览中集中展示活动终端、文件传输、端口转发、最近断联和 AI 诊断。
- 切换标签或分屏时，AI 面板明确显示当前绑定的会话和上下文来源，避免向错误主机发送建议。

### 3. 快速操作入口

- 增加命令面板，统一连接、切换标签、分屏、打开路径、启动转发和常用设置。
- 提供面向键盘的全局搜索，覆盖连接、书签、已打开会话和设置项。
- 支持把常用目录、命令片段和连接动作固定到主机快捷操作，但默认只插入、不自动执行。

### 4. 端口转发产品化

- 为已有本地 / 远程转发能力补齐创建、启动、停止、状态、错误和占用端口提示界面。
- 支持将转发配置绑定到连接，并明确区分“随连接启动”和“手动启动”。
- 在退出、断线和重连时保证转发生命周期可见且无孤儿监听端口。
- 记录启动时间、流量和最近错误，但不记录传输内容。

### 5. 远端健康快照

- 在用户主动授权后，通过只读命令采集远端 CPU、内存、磁盘、负载与基础系统信息。
- 将本机应用监控与远端主机快照明确分层，避免把两者混为同一状态。
- 支持从异常指标跳转到只读诊断 Agent，并附带数据时间与来源。

### 退出条件

- 用户可从一个连接入口完成终端、SFTP、转发、健康检查和 AI 诊断，无需重新选择主机或凭证。
- 高频动作可以通过命令面板完成，且所有跨功能跳转保留正确主机上下文。
- 端口转发具备完整的可见生命周期和失败恢复路径。
- 导入配置不会覆盖现有连接或秘密，除非用户在预览中明确确认。

---

## LATER — 可控的运维自动化（3–6 个月，2.3 目标）

目标：把一次性操作沉淀为可重复流程，同时不牺牲审批、证据与回滚能力。

### 1. 本地 Runbook

- 将命令、说明、变量、前置检查和预期结果保存为本地 Runbook。
- 区分只读检查、状态修改和破坏性操作三个风险等级。
- 执行前展示变量解析结果、目标主机、完整命令与影响范围。
- 支持逐步执行、暂停、跳过、失败停止和从安全步骤重试。
- Runbook 使用可审阅的文本格式，便于版本控制；秘密仅以钥匙串引用出现。

### 2. 多主机任务

- 基于连接标签选择目标，提供并发上限、分批执行和单主机熔断。
- 默认先运行只读预检，再允许进入修改步骤。
- 按主机隔离输出、退出码和失败原因，禁止把一台主机的上下文混入另一台。
- 对部分成功提供明确结果，不把“多数成功”包装为整体成功。

### 3. AI 辅助执行

- AI 先输出结构化计划、证据需求和风险等级，再生成命令。
- 每条建议关联其依据的终端输出或健康快照，并标明信息是否过期。
- 对修改型命令强制逐步批准；高风险命令同时要求影响说明与回滚建议。
- Agent 只通过受约束的本地执行接口工作，不开放不受限 shell 或静默后台执行。
- 支持将一次成功诊断整理为待审阅 Runbook，而不是直接形成自动执行规则。

### 4. 操作记录

- 形成按任务组织的本地时间线：目标、批准、命令、结果、传输和错误。
- 支持脱敏导出 Markdown / JSON，用于复盘与问题报告。
- 提供可配置保留周期和一键清除；默认不上传云端。

### 退出条件

- Runbook 和多主机任务在中断、部分失败与重试时结果可预测。
- 每个远端修改都能追溯到用户批准、具体命令、目标主机与执行结果。
- AI 不可绕过风险分级、审批、命令上限或秘密遮蔽策略。

验收状态（2026-08-23）：已通过。逐项证据、故障/恢复语义与回归门禁见 `docs/later-exit-criteria.md` 和 `docs/roadmap-audit.json`；本阶段已收束，不自动进入 EXPLORE。

---

## EXPLORE — 便携性与生态（6–12 个月，3.0 候选）

以下项目需要先通过用户需求和维护成本验证，不作为当前承诺：

- Linux x86_64 正式发行，以及 macOS Intel / Windows ARM 的需求评估。
- 安全的加密备份与设备迁移；优先本地文件，不默认引入账户体系。
- 连接、Runbook 和主题的扩展格式；先定义稳定数据契约，再评估插件 API。
- 基于真实使用场景评估动态 SOCKS 转发、串口、Mosh 或其他协议，一次只验证一个方向。
- 团队共享、集中策略与审计服务仅在个人工作区模型稳定后进入产品发现。

探索项进入正式阶段必须满足：有清晰用户群、可维护的跨平台实现、安全模型、升级策略和自动化测试方案。

首项审核状态（2026-08-24）：调研中，保持候选。Linux x86_64、macOS Intel 与 Windows ARM 均未满足全部准入条件；没有改动正式发行矩阵。用户群、维护成本、跨平台实现、安全模型、升级策略和自动化测试方案的逐项证据见 `docs/explore-portability-assessment.md` 与 `docs/roadmap-audit.json`。探索证据齐全后仍需独立评审，不自动转为承诺。

第二项审核状态（2026-08-24）：调研中，保持候选。现有连接导出、SQLite、Runbook、工作区、原生钥匙串、诊断与操作记录链路尚未形成一致的加密备份契约；直接用户需求、认证加密、跨存储回滚、升级夹具和跨平台迁移门禁也未满足。本次没有新增备份实现、账户或同步能力。范围清单、安全模型、失败/恢复语义和测试门槛见 `docs/explore-local-backup-assessment.md` 与 `docs/roadmap-audit.json`。

第三项审核状态（2026-08-24）：调研中，保持候选。稳定、纯数据契约是任何外部生产者或插件 API 的必要前置，但当前没有直接用户价值证据，连接 v1–v3、Runbook v1 和内置主题/无版本设置也尚未形成一致的大小/数量上限、双端校验、N-1 迁移和失败回滚证据。本次没有新增扩展格式或插件能力；插件 API 保持阻断。逐契约审计、安全/恢复边界与自动化门槛见 `docs/explore-extension-contracts-assessment.md` 与 `docs/roadmap-audit.json`。

第四项审核状态（2026-08-24）：调研中，保持候选。比较动态 SOCKS、串口、Mosh 与其他协议后，只选择受限的本地动态 SOCKS5 转发作为唯一研究对象；TermBridge 自身没有直接需求或真实试用证据，现有静态转发也缺少逐请求目标策略、大小/并发/超时上限、子连接强制取消、v4 升级夹具和两平台代理 E2E。本次没有实现任何协议或 UI，候选基础门禁保持阻断。比较、边界、失败/恢复语义与测试门槛见 `docs/explore-dynamic-socks-assessment.md` 与 `docs/roadmap-audit.json`。

第五项审核状态（2026-08-24）：调研中，保持候选。本次已关闭 Known Hosts 路径降级、AI API Key 写回 SQLite 和无版本终端恢复契约；Runbook、多主机、操作记录和脱敏导出也有本地证据。但个人工作区模型仍受完整数据库灾难恢复、当前提交的托管平台检查和正式平台原生凭据/恢复/故障 E2E 阻断，TermBridge 自身也没有可复核的团队需求。稳定后首个产品发现问题仅验证无秘密、待审阅的团队运维资料共享价值，集中策略和集中审计保持比较对象。本次没有新增账户、组织/成员、同步后端、策略执行、远程审计收集或托管服务。完整前置审计、方向比较、安全/恢复边界和自动化门槛见 `docs/explore-team-services-assessment.md` 与 `docs/roadmap-audit.json`。

---

## 暂不进入范围

- 完整代码编辑器、远程 IDE、容器编排控制台或云厂商资源管理平台。
- 无审批的 AI 自主运维、后台静默执行或默认开放写权限。
- 默认云同步凭证、终端输出、文件内容或 AI 对话。
- 为追求功能数量同时扩展多个远程协议。
- 在核心桌面链路稳定前建设移动端或 Web 端。

## 衡量指标

指标优先来自本地、脱敏且用户可见的数据；如未来引入遥测，必须明确选择加入。

| 维度 | 重点指标 |
| --- | --- |
| 可靠性 | 无崩溃会话时长、自动重连成功率、传输成功 / 恢复率、更新成功率 |
| 效率 | 从启动到连接耗时、从异常到首次有效诊断耗时、完成高频任务的交互步数 |
| 安全 | 秘密泄露回归数、未经批准执行数、主机密钥绕过数、高风险操作可追溯率 |
| 质量 | 主干通过率、端到端关键链路覆盖、跨平台缺陷逃逸数、测试告警数量 |
| 使用价值 | 周活跃主机数、工作区恢复使用率、跨终端 / SFTP 联动率、Runbook 复用率 |

## 路线图维护规则

- 每 4 周复盘一次 NOW，按退出条件而不是完成条目数量判断是否进入下一阶段。
- 新需求必须说明服务的主链路、用户价值、风险和被替代或推迟的项目。
- 发现安全、数据损坏、更新失败或跨平台阻断问题时，自动提升到 NOW。
- 每个阶段最多保留一个主要产品目标，避免同时推进“稳定性、平台扩展、协议扩展和团队化”。
- 完成阶段后记录实际结果与指标，再重排后续阶段；探索项不会自动变为承诺。
