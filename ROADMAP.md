# TermBridge Roadmap

> 定位：把 TermBridge 从「远程运维工具」升级为「AI 驱动的远程运维工作台」。
> 状态图例：`[ ]` 未开始 · `[~]` 进行中 · `[x]` 已完成

---

## 总览

四条主线，按依赖顺序推进：

| # | 主线 | 所在区域 | 目标版本 |
|---|------|---------|---------|
| 0 | AI 基础能力 | 全局（架构） | v2.1.x |
| 1 | 日志智能分析 | 工作台 | v2.2.x |
| 2 | 终端 AI 助手 | 终端区 | v2.3.x |
| 3 | SFTP 智能操作 | 文件区 | v2.4.x |
| 4 | Agent 深化与安全护栏 | 全局（持续） | 持续 |

**核心原则**（贯穿所有阶段，见文末展开）：

1. AI 生成的任何远程操作都必须显式人工确认，不搞无人值守。
2. API key 只进系统钥匙串，绝不落库。
3. 默认本地模型（Ollama），云端需用户显式配置 BYOK。
4. 复用现有架构范式（Zustand store、Tauri 事件流、i18n、keychainStore），不另起炉灶。

---

## 阶段 0：AI 基础能力（v2.1.x）

目标：打通 AI 能力的地基。后续所有功能都依赖本阶段的产出。

### 0.1 终端输出缓冲区

- **现状**：终端输出只活在 xterm 实例里，没有可供 AI 消费的中央数据源。
- **方案**：为每个会话维护环形输出缓冲区（保留最近 ~200KB 或 ~2000 行），在 `TerminalPane` 的 xterm `onData` 处写入；提供 `getRecentBuffer(sessionId)` 供 AI 上下文使用。
- **产出**：`src/stores/terminalBufferStore.ts` + 单测。
- **验收**：任意会话的最近输出可被读取；缓冲有上限；高频写入不引发重渲染（纯 store，不入 React 渲染树）。

### 0.2 AI 设置项

- `settings-panel.tsx` 新增「AI」区块：Provider、Model、流式开关、上下文长度。
- `types/index.ts` 新增 `AiProvider`、`AiModel` 类型；`appStore` 持久化。
- i18n 词条补全 `zh-CN` / `en-US`。
- **注意**：这是全新设置域，直接采用新格式，不承载任何旧数据迁移。

### 0.3 API Key → 系统钥匙串

- 复用 `keychainStore` / `CredentialManager` 范式（与现有 profile 密码一致），`invokeStoreAiApiKey` / `invokeRetrieveAiApiKey`，**绝不落库**。
- 前端只持有运行时内存中的 key，切换会话即清空。

### 0.4 Provider 抽象层

- Rust 侧新增 `ai_` 前缀命令组（符合 `commands.rs` 命名规范）：`ai_stream_chat`、`ai_list_models`、`ai_cancel_chat`。
- 统一接口支持两种后端：**Ollama 本地**（默认）与 **OpenAI 兼容协议 BYOK**；请求超时、错误归一化。
- 后端负责参数校验与 API key 注入，前端不接触 key（仅内存持有，见 0.3）。

### 0.5 流式输出通道

- 复用现有 Tauri event 模式（参照 `DataEvent` / 上传进度事件），AI 回答按 chunk 推送。
- 前端 `useAiChat` hook 订阅并聚合，支持中断（`ai_cancel_chat`）。

### 0.6 AI 面板骨架

- `src/components/ai/ai-panel.tsx`：可折叠侧边栏，对话输入 + 流式渲染（复用 `split-pane`、`scroll-area`、`textarea`）。
- `useAppShortcuts` 注册「打开/关闭 AI 面板」快捷键；`section-nav` 或标题栏提供入口。

**阶段验收**：一个能对话、能中断、能选择本地/云端的空 AI 面板跑通全链路；API key 只存在于钥匙串与内存；`tsc` 与全部单测通过。

---

## 阶段 1：工作台 · 日志智能分析（v2.2.x）

目标：AI 第一个落地产物。日志已结构化，输入质量最高，改动面最小。

### 1.1 日志上下文构造

- 复用 `log-panel.tsx` 的解析（`LOG_LINE_REGEX`）与过滤（date/level/source），把「当前筛选结果」作为 AI 上下文，与 UI 筛选状态保持实时一致（`logStore`）。

### 1.2 一键总结

- 按钮「AI 总结」：基于当前时间范围/级别过滤的日志生成报告——错误分布、根因假设、趋势。
- 结果可导出（复用 `invokeExportLogFile` 路径）。

### 1.3 错误聚类

- 对重复出现的错误（相同签名/堆栈）自动归组，给出「该错误出现 N 次，最早/最近时间，疑似根因」。

### 1.4 自然语言日志搜索

- 输入「认证失败相关的报错」→ AI 翻译为过滤条件（date/level/关键词）并执行 + 总结命中项。

### 1.5 单条日志解释

- 双击某条 ERROR（现有双击已绑定复制）→ 增加「解释这条错误」入口。

**阶段验收**：以上四个能力在 `log-panel.tsx` 内可用；上下文按筛选范围发送（不整文件全量发送）；测试覆盖上下文构造与请求参数生成。

---

## 阶段 2：终端 AI 助手（v2.3.x）

目标：终端主链路中最强感知的功能，复用阶段 0 的缓冲区。

### 2.1 Copilot 对话面板

- 对话自动携带当前会话上下文（阶段 0.1 缓冲区）+ 主机/用户信息。
- 支持「跟随当前标签页」：切换会话时上下文自动切换。

### 2.2 报错诊断

- 输出侧识别错误特征（`ERROR`、异常栈、常见运维报错），在面板浮出「要我解释吗」。
- 需要可控的触发频率（防打扰），默认手动触发，自动触发可关。

### 2.3 自然语言 → 命令

- 「查看磁盘占用」→ 生成 `df -h`，**用户确认后**回填到当前终端（见护栏）。
- 生成结果展示为「命令预览 + 确认/取消」，不直接执行。

### 2.4 选中文本解释

- xterm 选区右键菜单增加「Explain」；将选中文本 + 当前会话上下文发给 AI。

**阶段验收**：Copilot 与报错诊断在主流程可用；命令生成必须经过确认对话框；缓冲区读取有超时降级（AI 不可用时功能优雅降级，不影响正常使用）。

---

## 阶段 3：SFTP 智能操作（v2.4.x）

目标：把文件区从「手动浏览」升级为「语义操作」。

### 3.1 远程文件问答

- 复用 `invokeReadRemoteFile`（`src/types/index.ts:320`）作为工具：「这个 nginx.conf 配了什么」「package.json 里有哪些脚本」。
- 只读，不引入写路径。

### 3.2 目录语义理解

- 基于 `remoteEntries` / 选中路径做描述：「这个项目是什么结构」「在 /logs 下找昨天生成的 .log」。

### 3.3 自然语言批量操作

- 「把 /logs 下的 .log 打包下载」→ AI 生成**操作计划**（目标路径、筛选条件、动作），预览确认后复用现有传输模型（`transferStore` / `downloadRemotePaths`）执行。
- 仅「生成计划」，执行仍走现有、已确认的传输链路。

### 3.4 远程日志 / 大文件分析

- 大文件分段读取（tail/按行切片）送入 AI 总结，避免整文件内存/ token 爆炸。

**阶段验收**：3.1/3.2 只读能力可用；3.3 全程有操作计划确认；文件读取有大小上限与超时。

---

## 阶段 4：Agent 深化与安全护栏（持续）

目标：能力深化与安全机制迭代，随以上阶段同步演进。

- **工具调用闭环**：让 AI 能主动调用一组受限 Tauri 工具（读目录、读文件、只读命令），全部经过 `AIConfirmDialog` 批准。
- **执行确认对话框**：展示「目标主机 + 完整命令/操作 + 影响面」，用户批准后才执行；高危模式（`rm -rf`、`dd`、`mkfs` 等）强制二次确认。
- **行为留痕**：AI 每次操作写入后端日志（已有 `logStore`），可审计。
- **会话记忆（可选，本地）**：记录常用命令、主机偏好，仅存本地，不上传。
- **可观测性**：`/ai` 相关日志走已有 logger 分级，便于排查。

---

## 安全与隐私原则（贯穿所有阶段）

1. **密钥**：API key / 模型凭证只进系统钥匙串与进程内存，绝不落库、不进日志、不进 DB。
2. **内容脱敏**：发送给 AI 的上下文中剔除密码、私钥内容、密钥指纹敏感段；跳板机凭证绝不外发。
3. **最小化**：只发送 AI 完成任务所需的最小上下文（日志按筛选范围、文件按片段），支持手动关闭上下文共享。
4. **人工确认**：AI 生成的远程写入/执行操作一律先确认；只读操作也默认展示预览。
5. **本地优先**：默认 Ollama 本地推理；云端 BYOK 需要用户显式开启并知情。

---

## 非目标（近期不做）

- 不做 AI 全自动、无人值守的运维操作。
- 不做模型训练/微调/收集用户数据。
- 不做多模态（如截图识别远程界面）功能。
- 不做 AI 结果自动写入远程文件（仅生成计划 + 人工确认）。
